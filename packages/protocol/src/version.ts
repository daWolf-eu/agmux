// The agmux product version (single source of truth, shared by all binaries).
// Bump on each milestone and tag the release to match. Reported by `agmux -v`.
export const AGMUX_VERSION = "0.1.0-alpha.1";

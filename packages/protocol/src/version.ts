// The agmux product version (single source of truth, shared by all binaries).
// Bump on each milestone. Reported by `agmux -v`. The latest released git tag is
// `mvp-0.1.0`; this is the in-progress line beyond it.
export const AGMUX_VERSION = "0.2.0-dev";
